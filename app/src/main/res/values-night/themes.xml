<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SchoolManagementSystem" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/btnsBgColor</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/black</item>

        <item name="android:textColorHighlight">@color/handleAndTextSelectionColor</item>
        <item name="android:textSelectHandle">@color/handleAndTextSelectionColor</item>
        <item name="android:textSelectHandleLeft">@color/handleAndTextSelectionColor</item>
        <item name="android:textSelectHandleRight">@color/handleAndTextSelectionColor</item>
    </style>
</resources>